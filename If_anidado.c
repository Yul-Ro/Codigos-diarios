#include <stdio.h>
int main () {
    int anio;
    int sala;
    int por;
    puts("ingresa tu antiguedad");
         scanf( "%d", &anio );
    if ( anio >= 5)
    printf("Ingrese salario");
    scanf("%d" , &sala);
       if ( (sala*100)/10 > 1000) 
       { printf("felicidades tienes tu credito" );}
printf( "Fin del programa\n");
system ("Pause") ;
return 0;        
}